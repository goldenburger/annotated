[build]
  publish = "public"
  edge_functions = "netlify/edge-functions"

# Profiles and annotation pages are drawn in the browser by the same page.
[[redirects]]
  from = "/@*"
  to = "/index.html"
  status = 200
